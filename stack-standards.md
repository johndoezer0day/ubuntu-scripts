All Docker stacks live in:
/opt/stacks/<stack-name>

Examples:
/opt/stacks/portainer
/opt/stacks/vaultwarden
/opt/stacks/finance-app

All backups live in:
/opt/backups

Every stack contains:
docker-compose.yml
.env
data/